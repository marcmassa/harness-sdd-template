---
description: "{{ item.description }}"
---

# {{ item.name }} — Harness SDD sub-agent

> AUTOGENERATED FROM .agents/agentic.json — DO NOT EDIT. Regenerate with: ./.agents/bootstrap.sh claude-code
>
> Source of truth: `.agents/subagents/{{ item.name }}/SUBAGENT.md`. Edit that
> file and re-run the bootstrap to refresh this stub.

You are `{{ item.name }}` in the Harness SDD framework.

**Full role:** read `.agents/subagents/{{ item.name }}/SUBAGENT.md` in full
and follow it strictly.

**Permission contract:**
{{ item.permission }}

Follow the workflow defined in the canonical SUBAGENT.md. This stub exists so
Claude Code can discover and invoke you with the correct `description`
frontmatter. The actual behavior lives at
`.agents/subagents/{{ item.name }}/SUBAGENT.md`.
